syntax = "proto3";

message Product {
    string id = 1;
    string name = 2;
}

service ProductService {
    rpc RequestProductById (string) returns (Product)
    rpc AddProduct(AddProductInput) returns (Product)
}

message AddProductInput {
    string name = 1;
}